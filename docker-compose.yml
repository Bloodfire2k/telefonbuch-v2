version: '3.8'

services:
  telefonbuch:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      # Hier können die Umgebungsvariablen gesetzt werden
      # - CARDAV_SERVER_URL=https://your-nextcloud-server.com
      # - CARDAV_USERNAME=your-username
      # - CARDAV_PASSWORD=your-password
      # - USE_REAL_CARDAV=true
      # - ALLOWED_ADDRESSBOOKS=Vertreter,Edeka Adressbuch,Handwerker
    restart: unless-stopped 